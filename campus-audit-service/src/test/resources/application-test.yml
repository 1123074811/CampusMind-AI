spring:
  datasource:
    url: jdbc:h2:mem:campus_audit;MODE=MySQL;DATABASE_TO_UPPER=false;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver

campus:
  auth:
    jwt:
      issuer: campusmind-auth
      secret: test-secret-test-secret-test-secret-1234

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
