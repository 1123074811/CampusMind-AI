# llm 模式：启用 Spring AI ChatModel，通过 OpenAI 兼容协议接入 DeepSeek。
# 向量检索由 pg profile 启用，使用进程内真实 Transformer Embedding，不复用聊天接口伪造向量。
#
# 激活方式：--spring.profiles.active=llm,pg
#   - llm  : 本文件，配置 model/base-url/temperature
#   - pg   : application-pg.yml，配置 PostgreSQL/pgvector 与本地 Embedding
# API Key 使用环境变量 OPENAI_API_KEY。
campus:
  ai:
    mode: llm
    model-version: ${OPENAI_CHAT_MODEL:deepseek-v4-flash}
    prompt-version: llm-v1
    # pg profile 会覆盖为 pg；仅单独启动 llm 时保留内存降级能力。
    vector-store: memory

spring:
  ai:
    model:
      chat: openai
      # DeepSeek 不提供 embedding 端点；pg profile 使用独立的 Transformer Embedding。
      embedding: none
    openai:
      base-url: https://api.deepseek.com
      api-key: ${OPENAI_API_KEY}
      chat:
        options:
          model: ${OPENAI_CHAT_MODEL:deepseek-v4-flash}
          temperature: 0.2
  # 单独启用 llm 时仍不加载数据源；llm,pg 中后加载的 pg profile 会覆盖此列表。
  autoconfigure:
    exclude:
      - org.springframework.ai.model.openai.autoconfigure.OpenAiAudioSpeechAutoConfiguration
      - org.springframework.ai.model.openai.autoconfigure.OpenAiAudioTranscriptionAutoConfiguration
      - org.springframework.ai.model.openai.autoconfigure.OpenAiImageAutoConfiguration
      - org.springframework.ai.model.openai.autoconfigure.OpenAiModerationAutoConfiguration
      - org.springframework.ai.vectorstore.pgvector.autoconfigure.PgVectorStoreAutoConfiguration
      - org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
      - org.springframework.boot.autoconfigure.mongo.MongoAutoConfiguration
      - org.springframework.boot.autoconfigure.data.mongo.MongoDataAutoConfiguration
      - org.springframework.boot.autoconfigure.data.mongo.MongoRepositoriesAutoConfiguration
