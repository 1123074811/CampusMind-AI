name: campus_flutter_app
description: CampusMind mobile app skeleton.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_markdown: ^0.7.6
  url_launcher: ^6.3.2
  file_selector: ^1.1.0
  share_plus: ^13.2.0
  shared_preferences: ^2.5.3
  path_provider: ^2.1.5
  flutter_inappwebview: ^6.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/rain_bridge.js
